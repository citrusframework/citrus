<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>citrus-model</artifactId>
    <groupId>com.consol.citrus.model</groupId>
    <version>1.3</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <groupId>com.consol.citrus.model.testcase.ws</groupId>
  <artifactId>citrus-model-testcase-ws</artifactId>
  <name>citrus-model-testcase-ws</name>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <encoding>${file.encoding}</encoding>
        </configuration>
      </plugin>
      <!--
      TODO CD jaxb xsd2java generation fails with org.xml.sax.SAXParseException (Unique Particle Attribution) due to <xs:group ref="core:actionGroup"/> element
      <plugin>
        <groupId>org.jvnet.jaxb2.maven2</groupId>
        <artifactId>maven-jaxb2-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <extension>true</extension>
          <episodes>
            <episode>
              <groupId>com.consol.citrus.model.testcase.core</groupId>
              <artifactId>citrus-model-testcase-core</artifactId>
            </episode>
          </episodes>
          <catalog>src/main/resources/catalog.cat</catalog>
          <useDependenciesAsEpisodes>true</useDependenciesAsEpisodes>
          <schemaDirectory>src/main/resources/META-INF/schemas</schemaDirectory>
          <schemaIncludes>
            <include>**/*.xsd</include>
          </schemaIncludes>
          <bindingIncludes>
            <include>**/*.xjb</include>
          </bindingIncludes>
        </configuration>
      </plugin>
      -->
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>com.consol.citrus.model.testcase.core</groupId>
      <artifactId>citrus-model-testcase-core</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

</project>
