name: "CreateServiceTest"
author: "Christoph"
status: "FINAL"
description: Sample test in XML
actions:
  - kubernetes:
      client: "k8sClient"
      namespace: "test"
      createService:
        name: "my-service-1"

  - kubernetes:
      client: "k8sClient"
      namespace: "test"
      createService:
        name: "my-service-2"
        ports:
          - port: "80"
            targetPort: "8888"
        selector:
          labels:
            - name: "test"
              value: "citrus"
